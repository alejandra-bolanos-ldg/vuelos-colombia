[build]
builder = "nixpacks"
buildCommand = "cd backend/backend && mvn clean package -DskipTests"

[deploy]
startCommand = "cd backend/backend && java -jar target/backend-0.0.1-SNAPSHOT.jar"

[build.environment]
JAVA_VERSION = "17"